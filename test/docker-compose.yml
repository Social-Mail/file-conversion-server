# docker-compose.yml
version: "3"
services:
  app:
    # restart: on-failure
    container_name: social-mail-file-conversion-server
    image: social-mail-file-conversion-server
    build:
      context: ../
      dockerfile: Dockerfile
    environment:
      - PORT=80
    ports:
      - "9991:80"
